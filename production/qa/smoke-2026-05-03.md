# Smoke Check Report

**Date** : 2026-05-03 (re-run après fixes)
**Sprint** : Combat System (epic `production/epics/combat-system/`)
**Engine** : Godot 4.6
**QA Plan** : Not found — `/qa-plan sprint` recommandé pré-team-qa close-out
**Argument** : `sprint`
**Branch** : `chore/story-014-tech-debt-cleanup`

---

## Combat Sprint Status Snapshot

| State | Count | Stories |
|-------|-------|---------|
| **Complete** | 17 | 001-009, 011-016, 020, 022 |
| **Ready (hardware bench pending)** | 2 | 017 shapecast microbench, 018 integration soak frametime |
| **Blocked** | 3 | 010 tick-0 overlap (downstream gap), 019 feel playtest (Gap 5), 021 VFX decal (VFX dep) |

---

## Automated Tests

**Status** : **PASS** — 114 test cases | 0 errors | 0 failures | 0 flaky | 0 skipped | 0 orphans (`reports/report_341`, exit code 0).

**Scope** : tous les tests canoniques (filtre `! -name "* 2.gd"`) sous `tests/unit/combat/` + `tests/integration/combat/` — 19 test suites × ~6 tests moyenne, durée 1s 201 ms.

### Fixes appliqués (2026-05-03)

1. **`tests/unit/combat/scene_skeleton_invariants_test.gd`** — migration API GdUnit4 v5 :
   - `is_not_push_error()` (5 occurrences) → `is_success()` (l'ancienne API a été retirée).
   - `is_push_error("...")` → `is_runtime_error("Assertion failed: ...")` (les `assert(...)` GDScript en debug build émettent SCRIPT_ERROR pas push_error).
   - `CombatSystem.new()` → `(load(SCENE_PATH) as PackedScene).instantiate()` pour fournir le `@onready var _shape_cast = $ShapeCast3D` requis (sinon "Node not found" au _ready).

2. **`tests/unit/combat/state_machine_lifecycle_test.gd`** — fix capture-by-value lambda GDScript :
   - `var swing_ended_count: int = 0` → `var swing_ended_count: Array[int] = [0]` (Array passé par référence, int par valeur).
   - `func() -> void: swing_ended_count += 1` → `swing_ended_count[0] += 1`.
   - **Diagnostic** : trace debug a confirmé que `swing_ended.emit()` était bien appelé exactement 1 fois — le bug était dans la closure du test, PAS dans `combat_system.gd`. Pas de régression source code. Le test n'a probablement jamais correctement vérifié le compteur (latent bug dépassant la portée du sprint).

### Failures secondaires (workspace pollution) — INCHANGÉ

11 failures supplémentaires proviennent toujours des fichiers `* 2.gd` (280 fichiers untracked, 254 identiques au canonique + 26 divergents). Out of scope smoke (require Martin authorization for cleanup).

---

## Test Coverage

Toutes les stories Complete ont un fichier de test canonique :

| Story | Type | Test File | Coverage Status |
|-------|------|-----------|----------------|
| 001 scene-skeleton | Logic | `tests/unit/combat/scene_skeleton_invariants_test.gd` | COVERED ✅ (7/7 PASS) |
| 002 state-machine | Logic | `tests/unit/combat/state_machine_lifecycle_test.gd` | COVERED ✅ (6/6 PASS) |
| 003 death-respawn | Logic | `tests/unit/combat/death_respawn_lifecycle_test.gd` | COVERED |
| 004 attacked-handler | Logic | `tests/unit/combat/attacked_handler_buffer_test.gd` | COVERED |
| 005 build-capsule | Logic | `tests/unit/combat/build_capsule_basis_test.gd` | COVERED |
| 006 shapecast-collision | Logic | `tests/unit/combat/shapecast_collision_layers_test.gd` | COVERED |
| 007 sweep-aim | Logic | `tests/unit/combat/sweep_position_aim_guards_test.gd` | COVERED |
| 008 prev-position | Logic | `tests/unit/combat/prev_position_per_tick_test.gd` | COVERED |
| 009 anti-tunneling | Logic | `tests/unit/combat/anti_tunneling_substeps_test.gd` | COVERED |
| 011 single-hit-dedup | Logic | `tests/unit/combat/single_hit_kill_dedup_test.gd` | COVERED |
| 012 multi-hit-distance | Logic | `tests/unit/combat/multi_hit_distance_sort_test.gd` | COVERED |
| 013 slow-mo | Logic | `tests/unit/combat/slow_mo_wall_clock_test.gd` | COVERED |
| 014 mutual-kill | Integration | `tests/integration/combat/mutual_kill_death_pending_test.gd` | COVERED |
| 015 mid-swing | Integration | `tests/integration/combat/mid_swing_transitions_test.gd` | COVERED |
| 016 invariants | Logic | `tests/unit/combat/invariants_runtime_validation_test.gd` | COVERED |
| 020 audio-swoosh | Integration | `tests/integration/combat/{swoosh_fade_wall_clock,audio_multi_kill_ducking}_test.gd` | COVERED |
| 022 accessibility | Logic | `tests/unit/combat/accessibility_reduce_motion_test.gd` | COVERED |
| 010 BLOCKED | — | — | EXPECTED (Blocked) |
| 017 shapecast-microbench | Perf | `tests/perf/combat_shapecast_microbench.gd` (script créé, hardware run pending) | MANUAL |
| 018 integration-soak | Perf | `tests/integration/combat/integration_soak_test.gd` (PASS 2/2) + hardware Tier 1 frametime/draw_calls run pending | MANUAL |
| 019 BLOCKED | — | — | EXPECTED (Blocked Gap 5) |
| 021 BLOCKED VFX | — | — | EXPECTED (VFX dep) |

**Summary** : 17 COVERED ✅ (toutes PASS), 2 MANUAL (hardware bench), 3 EXPECTED (Blocked), 0 MISSING.

---

## Manual Smoke Checks

**Status** : NOT RUN — auto-mode session sans interaction directe Martin. Les 10 checks de `tests/smoke/critical-paths.md` doivent être confirmés manuellement avant team-qa hand-off.

| # | Check | Status |
|---|-------|--------|
| 1 | Game launches → main menu sans crash | NOT RUN |
| 2 | New session lançable depuis main menu | NOT RUN |
| 3 | Main menu répond à tous les inputs | NOT RUN |
| 4 | Movement (walk/jump/dash/wall-run) + caméra 60 fps | NOT RUN |
| 5 | `was_pressed_this_tick()` consommé tick courant | NOT RUN |
| 6 | Save game complete sans erreur | NOT RUN |
| 7 | Load game restaure l'état correct | NOT RUN |
| 8 | Pas de frame rate drops sur target hardware | NOT RUN |
| 9 | Croissance mémoire < 64 KB / 60 s | NOT RUN |
| 10 | Input-to-display latency p99 ≤ 16 ms | NOT RUN |

---

## Workspace Hygiene Concern (séparé du verdict)

**280 fichiers untracked `* 2.*`** dans le workspace (collision iCloud / multi-session) — INCHANGÉ depuis la première run :
- 254 byte-identiques au canonique (pure pollution)
- 26 divergents (potentielles WIP/snapshots à préserver — incluant `src/core/input_manager 2.gd`, `src/gameplay/combat/combat_system 2.gd`, `src/gameplay/level/level_system 2.gd`, `src/gameplay/player/Player 2.tscn` et 19 fichiers tests divergents)

**Action recommandée** : autorisation Martin pour `delete identiques + quarantaine divergents → _quarantine_dupes_2026-05-03/`. Bloque scan propre suite tests sans filtrage manuel.

---

## Verdict : **PASS WITH WARNINGS**

**Justification** : 0 failure canonique sur la suite combat (114/114 PASS). Les 2 tests précédemment FAIL ont été fixés (bugs de tests, pas de régression source). Le verdict reste PASS WITH WARNINGS car :

1. **Manual smoke checks NOT RUN** — auto-mode session, 10 checks critiques (game launch, save/load, perf 60fps, mémoire) pending confirmation interactive Martin.
2. **Workspace pollution non résolue** — 280 dupes `* 2.*` polluent les runs sans filtrage manuel ; pas de blocker fonctionnel mais bruit cumulatif.

### Hand-off conditional pass

`/team-qa sprint` peut démarrer DÈS QUE Martin confirme manuellement les 10 checks critical-paths (Batch 1+2 pour core stability + sprint mechanic ; Batch 3 save/load + perf). Workspace cleanup peut être fait en parallèle.

### Reste BLOCKING avant `/gate-check`

- Manual smoke checks Martin (10 entries → confirmer pass interactif)
- Hardware bench Tier 1 (story-017 shapecast microbench + story-018 integration soak frametime/draw_calls)
- Workspace dupes cleanup (280 fichiers — autorisation Martin)

---

**Prochaine étape recommandée** :
1. Martin lance les 10 manual smoke checks via Godot Editor (≤ 15 min) — confirmer PASS critical-paths.
2. Si PASS : `/team-qa sprint` Combat close-out → `/gate-check` pre-prod.
3. Workspace cleanup parallèle dès autorisation explicite.
